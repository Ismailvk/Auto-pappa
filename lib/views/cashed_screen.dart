import 'package:flutter/material.dart';

class CashedScreen extends StatelessWidget {
  const CashedScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Cashed Screen'),
      ),
    );
  }
}
